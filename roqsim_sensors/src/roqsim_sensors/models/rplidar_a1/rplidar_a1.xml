<mujoco model="rplidar_a1">
  <!--
    Slamtec RPLIDAR A1 (A1M8) 2D lidar: a standalone mount (housing mesh + a `scan` site) for the
    `spawn_sensor` plugin, mounted by a robot manifest at its vendor joint origin.

    Geometry from Clearpath's `turtlebot4_description` (urdf/sensors/rplidar.urdf.xacro), the scanner
    on the TurtleBot 4: the body is `rplidar_link`, the mesh has no visual origin, and the scan is
    stamped in the link itself.

    Body-local axes: x = the scan's zero bearing, z = up; the mesh spans z -46.1 .. +8.9 mm, so the
    vendor frame sits 8.9 mm below the top of the rotor housing.

    Built by external/convert/build_scanner_devices.py from turtlebot4 @ 7fd29fb420e906f3aca4a904adb54b69b11c7c00.
    Scan parameters and the `frames:` entry for this site are in rplidar_a1.manifest.yaml; see
    rplidar_a1_LICENSE for the mesh licence.
  -->
  <compiler angle="radian" meshdir="meshes" autolimits="true"/>

  <asset>
    <material name="rplidar_mat" rgba="0.1098 0.1098 0.1098 1"/>
    <mesh name="rplidar" file="rplidar.obj"/>
  </asset>

  <worldbody>
    <body name="mount">
      <inertial pos="0 0 0" mass="0.17" diaginertia="0.00019267 0.00012241 0.00021308"/>
      <geom name="rplidar_a1_visual" type="mesh" mesh="rplidar" material="rplidar_mat"
            contype="0" conaffinity="0"/>
      <!-- The vendor's own collision box (7.1 x 10 x 6 cm). -->
      <geom name="rplidar_a1_collision" type="box" pos="0 0.013 -0.019" size="0.0355 0.05 0.03" group="3"/>
      <!-- The vendor scan frame (rplidar_link) is the mount itself.
           Keep in step with the manifest's `frames:` entry. -->
      <site name="scan" pos="0 0 0" size="0.005"/>
    </body>
  </worldbody>
</mujoco>
